<div class="flex flex-col w-full">
  <div class="text-slate-500">Buscar remitente:</div>
  <%= form_with(scope: :remitent, url: filter_remitents_path(sell_id: @sell.id), id: 'filter_remitents', class: 'flex items-center w-full gap-3', method: :post) do |form| %>
    <%= form.text_field :name, placeholder: 'e.g. Roberto López', class: "input-tailwindcss" %>
    <button class="rounded-full py-2 px-3 bg-primary text-white font-semibold cursor-pointer shadow">
      <i class="fa-solid fa-magnifying-glass"></i>
    </button>
  <% end %>
</div>
