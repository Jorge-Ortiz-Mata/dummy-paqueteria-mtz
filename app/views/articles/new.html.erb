<section class="flex flex-col h-full w-full">
  <%= render 'shared/navbar' do %>
    <h1 class="font-bold text-2xl text-sidebarTextColor">Nuevo articulo</h1>
  <% end %>

  <div class="flex flex-col xl:flex-row px-2 sm:px-5 py-5 w-full gap-5">
    <div class="flex flex-col overflow-y-auto px-2 sm:px-5 py-5 gap-10 bg-white border rounded shadow w-full xl:w-1/2">
      <%= render 'shared/breadcumb', { path: articles_path, path_text: 'Articulos', text: 'Nuevo articulo' } %>

      <%= render "form", { article: @article, btn_title: 'Crear articulo', cancel_path: articles_path } %>
    </div>

    <%= render 'estimated_price_form' %>
  </div>

  <div class="flex flex-col xl:flex-row px-2 sm:px-5 py-5 w-full gap-5">
    <%= render 'default_prices/default_prices', { default_prices: @box_default_prices, title: 'Precios establecidos - Cajas de cartón' } %>
  </div>
</section>


