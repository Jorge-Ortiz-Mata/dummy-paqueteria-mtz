<div class="flex items-center justify-between rounded-lg py-5 px-2 xl:px-5 bg-white shadow border w-full duration-300">
  <div class="text-xl font-bold text-sidebarTextColor flex items-center gap-3">
    <%= yield %>
    <h6><%= title %></h6>
  </div>
  <p class="font-semibold text-2xl xl:text-3xl">
    <%= text %>
  </p>
</div>
